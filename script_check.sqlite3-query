-- database: ./sky.db

-- Use the ▷ button in the top right corner to run the entire file.

SELECT * FROM airports;


select COUNT(*) AS Number_of_airports , country from airports group by country order by COUNT(*) desc Limit 10;

